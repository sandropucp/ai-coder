{
  "project": "PM MVP",
  "database": "SQLite",
  "tables": [
    {
      "name": "users",
      "columns": [
        { "name": "id", "type": "INTEGER", "constraints": ["PRIMARY KEY", "AUTOINCREMENT"] },
        { "name": "username", "type": "TEXT", "constraints": ["UNIQUE", "NOT NULL"] },
        { "name": "password", "type": "TEXT", "constraints": ["NOT NULL"] }
      ]
    },
    {
      "name": "boards",
      "columns": [
        { "name": "id", "type": "INTEGER", "constraints": ["PRIMARY KEY", "AUTOINCREMENT"] },
        { "name": "user_id", "type": "INTEGER", "constraints": ["NOT NULL", "FOREIGN KEY REFERENCES users(id)"] },
        { "name": "name", "type": "TEXT", "constraints": ["NOT NULL"] }
      ]
    },
    {
      "name": "columns",
      "columns": [
        { "name": "id", "type": "TEXT", "constraints": ["PRIMARY KEY"] },
        { "name": "board_id", "type": "INTEGER", "constraints": ["NOT NULL", "FOREIGN KEY REFERENCES boards(id)"] },
        { "name": "title", "type": "TEXT", "constraints": ["NOT NULL"] },
        { "name": "position", "type": "INTEGER", "constraints": ["NOT NULL"] }
      ]
    },
    {
      "name": "cards",
      "columns": [
        { "name": "id", "type": "TEXT", "constraints": ["PRIMARY KEY"] },
        { "name": "column_id", "type": "TEXT", "constraints": ["NOT NULL", "FOREIGN KEY REFERENCES columns(id)"] },
        { "name": "title", "type": "TEXT", "constraints": ["NOT NULL"] },
        { "name": "details", "type": "TEXT" },
        { "name": "position", "type": "INTEGER", "constraints": ["NOT NULL"] }
      ]
    }
  ]
}
